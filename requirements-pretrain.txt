# Pix2Rep-v2 SSL pretraining — additional dependencies
# Install with: pip install -r requirements-pretrain.txt

torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24
rasterio>=1.3           # load PRISMA GeoTIFF files
h5py>=3.8               # optional patch cache (speeds up DataLoader)
scipy>=1.11             # patch extraction utilities
zenodo-get>=1.3         # download MUMUCD dataset
tensorboard>=2.14       # optional training visualisation
